# Session Notes: 2026-03-23

## Done

- Added a new zero-tool core skill at `skills/first-principles-scientific-mindset/`.
- Encoded the core reasoning rules:
  - derive from fundamentals
  - make assumptions explicit
  - separate facts, assumptions, and conclusions
  - form falsifiable hypotheses
  - validate against evidence, tests, measurements, or primary sources
- Added explicit anti-injection constraints so retrieved or quoted content is not treated as trustworthy by default.
- Updated example and schema docs so `first-principles-scientific-mindset` is shown in `core_skills`.
- Updated repo config to treat this as a default core skill in the checked-in config.
- Verified live GearCore behavior with the installed CLI:
  - the new skill is not visible just because it exists in this repo
  - after `gearcore add-skill ... --symlink`, it becomes visible on the next `gearcore list-skills`
  - `gearcore request-skill first-principles-scientific-mindset` loads successfully
  - a running `gearcore serve` process does not currently hot-reload newly added skills automatically

## Live Environment Note

- The skill was symlink-registered into `~/.config/gearcore/skills/first-principles-scientific-mindset` for runtime verification.
- The live user config at `~/.config/gearcore/config.yaml` still has `core_skills: []`, so the skill is discoverable but not auto-activated in the installed CLI.

## Tomorrow

- Decide whether the installed user config should auto-activate `first-principles-scientific-mindset`.
- Decide whether the checked-in `README.md` changes should be reviewed and committed separately from this skill work.
- If true hot reload is desired for long-running `gearcore serve`, add an exposed refresh path:
  - CLI command, MCP tool, or filesystem watcher
  - call `SkillManager.refresh()` and re-apply core-skill activation
- Consider adding a small regression test around skill discovery and post-registration refresh behavior.
- Decide whether this reasoning skill should remain one combined core skill or split into two narrower skills:
  - `first-principles`
  - `scientific-method`
