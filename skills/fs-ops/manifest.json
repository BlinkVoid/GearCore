{
  "name": "fs-ops",
  "version": "1.0.0",
  "description": "Basic filesystem operations like listing and reading files.",
  "category": "system",
  "mcp_servers": [
    {
      "server_id": "local-fs",
      "tools": ["list_directory", "read_file", "list_allowed_directories"]
    }
  ],
  "activation": {
    "strategy": "manual",
    "triggers": ["files", "directory", "read", "list"]
  }
}
